name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull & redeploy
        run: |
          cd /opt/vedurstod-turbo
          git pull origin main
          docker compose up -d --build vedur
